export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Employee CRUD Application",
  description:
    "Employee CRUD Application using Firebase Firestore",
  mainNav: [
    {
      title: "Home",
      href: "/",
    },
  ]
}